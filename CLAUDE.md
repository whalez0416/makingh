# 디토셀의원 홈페이지 재구축 — 통합 브리프 v3

(VI 후보 3안 병행 + 뷰웰 레이아웃 차용)

전체를 읽고 Phase 순서대로 진행한다.

## 0. 배경 (반드시 이해)

- 기존 사이트(dittocell.com, Next.js+Vercel)의 원본 코드를 인수할 수 없어 처음부터 새로 만든다.
- 기존 사이트의 코드는 한 줄도 복사하지 않는다. `/reference`의 HTML 소스는 콘텐츠(텍스트·4개 언어 번역)와 정보 추출용이다. 콘텐츠는 병원 소유 자산이므로 그대로 사용.
- 레이아웃 참고: beauwell.kr의 섹션 구성·서사 흐름만 차용한다 (§4). 뷰웰의 시각 스타일·색·폰트·코드는 참고하지 않는다.
- 로고: 병원 제공 원본 사용, 임의 재디자인 금지. 수급 전엔 워드마크 "DITTOCELL" 텍스트로 임시 처리 (서체는 선택된 VI 규정을 따름).

## 1. 기술 스택 (변경 금지)

- Next.js 최신 안정, App Router, TypeScript / Tailwind CSS
- Payload CMS 3.x — 같은 앱에 통합. DB 로컬 SQLite → 추후 Postgres 전환 가능 구조
- next-intl — ko/en/zh/ja, `/ko/...` URL
- 슬라이더 embla-carousel / 배포 타깃 Vercel (현재는 로컬만)

## 2. 브랜드 컨셉 — "Ditto" (모든 VI 공통)

Ditto = 위와 동일, 다시 한 번. 줄기세포의 자가복제(self-renewal)가 곧 병원 이름이다.
핵심 문장: **"건강한 세포가 건강한 세포를 복제합니다 — 당신의 가장 건강했던 피부와 동일하게, 다시."**
공통 모티프 재료: 〃(ditto mark) 와 겹친 두 원(복제 직후의 동일한 두 세포). 각 VI가 이를 다르게 활용한다.

## 3. VI 후보 3안 — **색상만** (2026-08-20 방침 변경)

> **발주자 지시로 방침이 바뀌었다.**
> 레이아웃·그리드·박스 위치·타이포 스케일·라운딩은 **beauwell.kr 실측값으로 통일**하고,
> VI 3안은 **색 팔레트만** 갈아끼운다. 서체는 Pretendard 하나로 통일한다.
> 아래 각 안의 서체·모티프·라운딩·히어로 문법 규정은 **적용하지 않는다** — 색상 값만 유효하다.
> 실측 수치는 `docs/beauwell-analysis.md` 참조.

| 안 | bg | surface | ink | sub | accent | line |
| --- | --- | --- | --- | --- | --- | --- |
| A 앤틱골드 | `#F6F2EA` | `#FFFDF9` | `#221D14` | `#6E6455` | `#9E7C40` | `#EAE2D2` |
| B 딥그린 | `#F4F6F5` | `#FFFFFF` | `#101614` | `#4E5A55` | `#0E5A4A` | `#DCE3E0` |
| C 모카골드 | `#FCFBF9` | `#FFFFFF` | `#26221D` | `#7C7468` | `#A78B6F` | `#EDE8E0` |

<details><summary>원래의 3안 규정 (색상 외에는 보류)</summary>

## 3. VI 후보 3안 — 테마 토큰 아키텍처로 구현

구현 방식: 모든 색·서체·라운딩은 CSS 변수(`--color-primary` 등)로 선언하고, 3안을 각각 테마 파일로 정의한다.
Phase 1에서 메인 히어로+숫자 스트립+자가진단 섹션을 3개 테마로 모두 렌더링(테마 스위처 또는 `/theme-a,b,c` 라우트)해
비교 후, 발주자가 1개를 확정하면 해당 테마로 고정하고 나머지는 제거한다.

### 테마 A — "Ditto Mark 〃" (에디토리얼 · 다시, 그때의 피부)
- 무드: 문학적 에디토리얼. 〃 기호가 브랜드의 얼굴
- 컬러: bg `#F6F2EA` / surface `#FFFDF9` / ink `#221D14` / sub `#6E6455` / accent(앤틱골드) `#9E7C40` / border `#EAE2D2`
- 서체: 국문 디스플레이 Noto Serif KR(500), 본문 Pretendard(400), 영문·기호 Cormorant Garamond(italic 적극 사용)
- 모티프: 〃를 히어로 배경 초대형 워터마크(투명도 ~13%), 리스트 불릿, eyebrow 접두 기호로 반복
- 헤드라인 문법: "당신의 가장 건강했던 세포, 〃 다시 한 번." — 〃가 문장 성분으로 작동
- 버튼: 사각(라운딩 0), ink 배경 → hover 시 accent. 보조 링크는 밑줄형
- 히어로: 사진 없이 타이포+워터마크 중심 (사진은 서브 섹션에서)

### 테마 B — "Bio Modern" (바이오테크 정밀)
- 무드: 헬스케어 테크. 세리프 없음, 데이터 문법
- 컬러: bg `#F4F6F5` / surface `#FFFFFF` / ink `#101614` / sub `#4E5A55` / primary(그린) `#0E5A4A` / border `#DCE3E0`~`#E5EAE8`
- 서체: 국문·영문 Pretendard (헤드라인 800, letter-spacing -0.035em), 데이터 레이블 IBM Plex Mono (uppercase, 11~12px)
- 모티프: 도트 그리드 배경(28px 간격, 마스크 페이드) + 겹친/회전하는 서클 라인(1.5px, primary, 낮은 투명도, 60s 저속 회전)
- 배지: 모노스페이스 스테이터스 배지 ("ADVANCED REGENERATIVE MEDICINE · SEOUL" + 그린 도트)
- 카드: 라운딩 10px, 연한 배경+보더, 그림자 없음. 스펙 카드는 [모노 레이블 → 대형 볼드 수치 → 설명] 구조
- 버튼: 라운딩 8px, primary 솔리드

### 테마 C — "Porcelain Gallery" (사진 주도 미니멀)
- 무드: 화이트 갤러리. 피부 사진이 주인공, 텍스트는 캡션처럼
- 컬러: bg `#FCFBF9` / ink `#26221D` / sub `#7C7468`~`#8C8377` / accent(모카골드) `#A78B6F` / hairline `#EDE8E0`
- 서체: 국문 디스플레이 Noto Serif KR(500, line-height 1.55), 본문 Pretendard(300~400), 로마숫자·이탤릭 레이블 Cormorant Garamond
- 모티프: 겹친 두 원을 사진과 텍스트의 경계 오브제로 (한 원은 배경색 채움, 한 원은 라인만). 섹션 넘버는 로마숫자 이탤릭(i. ii. iii.)
- 레이아웃 특성: 히어로는 화면 45~50%를 사진이 차지(스플릿), 구분은 그림자·박스 없이 헤어라인 1px만. 여백 최대치
- 버튼: 텍스트+밑줄형이 기본. 솔리드 버튼 최소화
- 사진 의존도가 가장 높음 — 사진 수급 전엔 웜 베이지 그라데이션 플레이스홀더

</details>

### 공통 규칙 (3안 모두)
- 숫자의 주인공화: "210+", "90%", "2~3기압" 등 수치는 각 테마의 디스플레이 문법으로 크게
- 모션: 섹션 진입 fade+8px 상승 0.5s 1회. scale 호버 금지. `prefers-reduced-motion` 존중
- 골드/액센트 면적 5% 이하. 그라데이션 블러 덩어리·반짝이 효과·스톡 DNA 이미지 금지
- 포토 디렉션: 피부 매크로(자연광·저채도·웜WB), 미니멀 클리닉 인테리어(웜 우드+화이트), 현미경 세포 추상. 오버레이는 블랙 대신 테마 컬러 틴트 25~35%

### 반응형 (필수)
- **모바일 퍼스트로 작성** — 기본 스타일이 모바일, 브레이크포인트로 확장 (Tailwind 기본 문법 그대로: `md:` `lg:` 접두사)
- 브레이크포인트: 모바일 기본 / `md` 768px / `lg` 1024px / 컨테이너 `max-w` 1180px
- **검증 기준 뷰포트: 375px(폰) / 768px(태블릿) / 1440px(데스크톱)** — 각 Phase 완료 보고 시 3개 폭 확인 필수
- 모바일 헤더: GNB → 햄버거 + 풀스크린 오버레이 메뉴 (언어 스위처 포함), 예약 CTA는 유지
- 터치 타깃 최소 44px, 플로팅 버튼은 모바일에서 축소·병합 가능
- 테마별 모바일 규칙:
  - A안 〃 워터마크는 축소·투명도 하향
  - B안 도트 그리드는 유지하되 서클 장식 축소
  - C안 사진 스플릿은 모바일에서 상하 스택(사진 위, 텍스트 아래) — **사진 숨김 처리 금지** (사진이 주인공인 테마라 숨기면 정체성이 사라짐)
- 표·패키지 인덱스 리스트: 모바일에서 열 축소 (카테고리 태그 숨기고 번호+제목+화살표만)
- 갤러리 마퀴·슬라이더: 모바일에서 카드 폭 85vw, 스와이프 제스처

## 4. 레이아웃 — beauwell.kr 서사 구조 차용

뷰웰 메인의 검증된 스크롤 서사를 디토셀 콘텐츠로 재구성한다. 차용은 섹션의 종류·순서·역할이며, 시각 표현은 §3 테마가 결정한다.

### 메인 페이지 섹션 흐름
1. **히어로** — 풀스크린. 테마별 문법(A: 타이포+〃 워터마크 / B: 그리드+서클 / C: 사진 스플릿)
2. **숫자 스트립** — 210+ / 90% / 첨단재생의료 실시기관
3. **철학 헤드라인 교차 섹션** (뷰웰의 "처방전과 숫자 너머…" 패턴) — 큰 문장 하나 + 클리닉 한 줄 소개를 시그니처/줄기세포/안티에이징 3회 교차. 문장은 reference의 병원 카피에서 발췌·정제
4. **자가진단 섹션 ★** (뷰웰의 "내 몸에 필요한 프로그램" 패턴) — 환자 고민 문장 → 치료 연결 카드:
   - "시술을 받아도 금방 처지는 것 같아요" → 줄기세포 재생
   - "티 나는 시술은 부담스러워요" → 시그니처 패키지
   - "만성피로에 피부까지 푸석해요" → 호밍셀 IV
   - "리프팅은 아플까 봐 겁나요" → 소프웨이브
   (Q→A 구조는 AEO 핵심 자산. reference의 후기에서 환자 언어를 추가 발굴해 6~8개까지 확장 가능)
5. **시그니처 패키지 프리뷰** (뷰웰의 넘버링 패키지 패턴) — 01, 02… 번호 인덱스 리스트 4~5개 + 전체보기
6. **전문 클리닉 그리드** (뷰웰의 탭 필터 그리드 패턴, 축소판) — 전체/시그니처/안티에이징/줄기세포 탭 + 카드. 메뉴가 5개뿐이므로 2뎁스 메가메뉴는 만들지 않는다
7. **고객 후기** — 인스타 릴스 카드 슬라이더 (CMS reviews)
8. **의료진** — 대표원장 사진 + 철학 인용문 + 경력
9. **소식 피드** (뷰웰의 News 패턴) — CMS notices 최신 + 인스타 링크
10. **시설 갤러리 마퀴** (뷰웰의 Gallery 패턴) — 자동 흐르는 이미지 띠, 저속
11. **클로징 스테이트먼트 + 예약 CTA** (뷰웰의 마무리 선언 패턴)
12. **오시는길 정보 블록** — 주소/지도 링크/대표전화/진료시간 (site-settings)

뷰웰에서 차용하지 않는 것: 메가메뉴, 동시 다중 팝업, 과다 플로팅 버튼

### 공통 레이아웃 (root layout — 전 페이지 필수)
- 헤더: 투명→스크롤 시 배경. 로고 / GNB 5개(시그니처·안티에이징·줄기세포·병원소개·온라인 예약) / 언어 스위처 / 예약 CTA
- 푸터·플로팅(카톡·전화·TOP): 전 페이지. 데이터는 site-settings에서

### 서브 페이지
| 경로 | 구성 |
| --- | --- |
| `/[locale]/signature` | 패키지 13종 — 번호 인덱스 리스트 |
| `/[locale]/anti-aging` | 탭 4: 레이저(9)/주사(3)/스킨부스터(4)/고압산소 |
| `/[locale]/stem-cell` | 탭 4: 개요/자가혈/자가지방/고압산소 + 기능 6종·시술 과정 |
| `/[locale]/about` | 철학·시설·원장 인사말 |
| `/[locale]/reviews` | 인스타 릴스 카드 (CMS) |
| `/[locale]/notice` | 목록+상세 (CMS) |
| `/[locale]/reservation` | Phase 5 전까지 카카오톡 예약 안내 |

## 5. Payload CMS (비개발자 관리자 기준, 과설계 금지)

- `notices` — 제목/본문(리치텍스트)/분류(공지·이벤트)/게시일/노출. ko 필수, en·zh·ja 선택(미입력 시 ko 표시)
- `popups` — 제목/이미지 또는 리치텍스트/노출기간(시작~종료)/on-off. 기간 경과 자동 미노출
- `reviews` — 인스타 URL/썸네일/하이라이트/설명/정렬/노출
- `media` — 업로드
- globals: `site-settings` — 전화·주소·진료시간·SNS

## 6. 기존 사이트 결함 수정 (전부 필수)

1. GNB/푸터/플로팅 root layout 배치 (기존: 메인에만 존재)
2. 더미 전화번호 금지 — 모든 `tel:`은 site-settings의 02.564.7774
3. 페이지·언어별 `generateMetadata` (기존: 전 페이지 동일)
4. `sitemap.xml`(전 언어×전 페이지) / `robots.txt` / canonical / hreflang(4언어+x-default) / JSON-LD(MedicalClinic+Physician)
5. `next/image`, quality 75~80, `priority`는 히어로 첫 장만

## 7. 고정 정보 (site-settings 초기값)

디토셀의원 / 대표 성용완(의학박사) / 사업자 140-90-04585 / 서울 강남구 언주로 554, 3층 /
TEL 02.564.7774 / FAX 02.564.7775 / 화·수 10-19 · 월·금 10-20 · 토 10-16 · 점심 13-14 · 목·일 휴진 /
카카오 pf.kakao.com/_EGEgn / 인스타 @ditto.cell

## 8. Phase

- **Phase 1 뼈대+VI 비교**: 셋업, 테마 토큰 아키텍처, root layout, 메인 섹션 1·2·4를 테마 A/B/C 3버전으로 렌더링 → 발주자 비교·확정 → 확정 테마로 고정, 나머지 테마 제거
- **Phase 2 CMS 검증**: Payload+notices → 관리 화면 등록 → `/notice` 반영 확인 → popups·reviews·site-settings 확장, 메인 7·9·12를 CMS 전환
- **Phase 3 메인 완성+서브**: 메인 나머지 섹션(3·5·6·8·10·11), 서브 5개 (ko)
- **Phase 4 다국어·SEO**: 번역 연결(reference 추출), 언어 스위처, §6-3·4 전체
- **Phase 5 예약** (별도 지시 전 착수 금지): 예약 플로우+컬렉션+관리 화면, 알림톡/SMS 스텁

## 9. 작업 규칙

- Phase 시작 전 계획 브리핑, 완료 시 실행 명령어·확인 포인트 보고
- 컴포넌트화: `SectionHeader`, `PackageRow`, `DiagnosisCard`, `ClinicCard`, `ConsultCta`, `CellSymbol`, `DittoMark` 등
- 최신 API 추측 금지, 공식 문서 확인 (특히 Payload 3.x × Next.js)
- 커밋은 의미 단위. README에 셋업·환경변수·CMS 접속법 (인수인계 대비)
- 의료광고: 치료 효과 단정·보장 문구 생성 금지. reference 원문 표현 범위 내에서만
